# Example Cargo project

To build the project: `cargo build [--release] [--features my_testing_flag]`

To run the executable: `cargo run [--release] [--features my_testing_flag] -p driver`

To run the tests: `cargo test -p fraction`
